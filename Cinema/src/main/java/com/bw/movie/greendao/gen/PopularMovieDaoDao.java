package com.bw.movie.greendao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.bw.movie.bean.PopularMovieDao;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "POPULAR_MOVIE_DAO".
*/
public class PopularMovieDaoDao extends AbstractDao<PopularMovieDao, Void> {

    public static final String TABLENAME = "POPULAR_MOVIE_DAO";

    /**
     * Properties of entity PopularMovieDao.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property FollowMovie = new Property(0, int.class, "followMovie", false, "FOLLOW_MOVIE");
        public final static Property Id = new Property(1, int.class, "id", false, "ID");
        public final static Property ImageUrl = new Property(2, String.class, "imageUrl", false, "IMAGE_URL");
        public final static Property Name = new Property(3, String.class, "name", false, "NAME");
        public final static Property Rank = new Property(4, int.class, "rank", false, "RANK");
        public final static Property Summary = new Property(5, String.class, "summary", false, "SUMMARY");
        public final static Property Flag = new Property(6, boolean.class, "flag", false, "FLAG");
    }


    public PopularMovieDaoDao(DaoConfig config) {
        super(config);
    }
    
    public PopularMovieDaoDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"POPULAR_MOVIE_DAO\" (" + //
                "\"FOLLOW_MOVIE\" INTEGER NOT NULL ," + // 0: followMovie
                "\"ID\" INTEGER NOT NULL ," + // 1: id
                "\"IMAGE_URL\" TEXT," + // 2: imageUrl
                "\"NAME\" TEXT," + // 3: name
                "\"RANK\" INTEGER NOT NULL ," + // 4: rank
                "\"SUMMARY\" TEXT," + // 5: summary
                "\"FLAG\" INTEGER NOT NULL );"); // 6: flag
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"POPULAR_MOVIE_DAO\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, PopularMovieDao entity) {
        stmt.clearBindings();
        stmt.bindLong(1, entity.getFollowMovie());
        stmt.bindLong(2, entity.getId());
 
        String imageUrl = entity.getImageUrl();
        if (imageUrl != null) {
            stmt.bindString(3, imageUrl);
        }
 
        String name = entity.getName();
        if (name != null) {
            stmt.bindString(4, name);
        }
        stmt.bindLong(5, entity.getRank());
 
        String summary = entity.getSummary();
        if (summary != null) {
            stmt.bindString(6, summary);
        }
        stmt.bindLong(7, entity.getFlag() ? 1L: 0L);
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, PopularMovieDao entity) {
        stmt.clearBindings();
        stmt.bindLong(1, entity.getFollowMovie());
        stmt.bindLong(2, entity.getId());
 
        String imageUrl = entity.getImageUrl();
        if (imageUrl != null) {
            stmt.bindString(3, imageUrl);
        }
 
        String name = entity.getName();
        if (name != null) {
            stmt.bindString(4, name);
        }
        stmt.bindLong(5, entity.getRank());
 
        String summary = entity.getSummary();
        if (summary != null) {
            stmt.bindString(6, summary);
        }
        stmt.bindLong(7, entity.getFlag() ? 1L: 0L);
    }

    @Override
    public Void readKey(Cursor cursor, int offset) {
        return null;
    }    

    @Override
    public PopularMovieDao readEntity(Cursor cursor, int offset) {
        PopularMovieDao entity = new PopularMovieDao( //
            cursor.getInt(offset + 0), // followMovie
            cursor.getInt(offset + 1), // id
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // imageUrl
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3), // name
            cursor.getInt(offset + 4), // rank
            cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5), // summary
            cursor.getShort(offset + 6) != 0 // flag
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, PopularMovieDao entity, int offset) {
        entity.setFollowMovie(cursor.getInt(offset + 0));
        entity.setId(cursor.getInt(offset + 1));
        entity.setImageUrl(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setName(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
        entity.setRank(cursor.getInt(offset + 4));
        entity.setSummary(cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5));
        entity.setFlag(cursor.getShort(offset + 6) != 0);
     }
    
    @Override
    protected final Void updateKeyAfterInsert(PopularMovieDao entity, long rowId) {
        // Unsupported or missing PK type
        return null;
    }
    
    @Override
    public Void getKey(PopularMovieDao entity) {
        return null;
    }

    @Override
    public boolean hasKey(PopularMovieDao entity) {
        // TODO
        return false;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
